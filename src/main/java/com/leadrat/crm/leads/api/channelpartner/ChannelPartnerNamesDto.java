package com.leadrat.crm.leads.api.channelpartner;

import java.util.UUID;

/** Lightweight pair for partner pickers and the lead filter dropdown. */
public record ChannelPartnerNamesDto(UUID id, String name) {
}
